use std::env;
use std::process;

fn main() {
 
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        eprintln!("Error: Please provide a Cypher query string, not enough arguments");
        process::exit(1);
    }

    let query = &args[1];
    
    //data received proof

    println!("SQL Generated by Rust Transformer");
    println!("/* Source: {} */", query);
    println!("SELECT * FROM placeholder_table;");
}